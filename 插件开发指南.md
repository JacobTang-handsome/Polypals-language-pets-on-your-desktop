# PolyPals 插件开发指南

0.4 版只支持声明式 `.polypals-pack` 目录，不执行脚本、动态库或任意 Swift 代码。包内使用 `manifest.json`，内容放在 `content/`、`assets/` 或 `localizations/`，并包含 `LICENSE` 和 `README.md`。

请从 `Examples/ContentPack` 或 `Examples/PetPack` 复制示例，使用稳定的反向域名 ID 和语义化版本，声明最低应用版本、作者、许可证、语言、CEFR 等级和能力。除 `manifest.json` 外的每个文件都必须提供 SHA-256 摘要，路径必须是包内相对路径。

内容卡要声明最低、推荐和最高等级；文化卡必须附可核验来源，对白必须为原创或有授权。宠物包不得使用 Sol、Mousse、Ash 的稳定 ID；Sprite v2 必须是 192×208 单元、8×11、1536×2288、透明背景、11 行动画和 16 个视线方向。

验证命令：

```sh
Tools/validate-pack Examples/ContentPack/ExampleContent.polypals-pack
Tools/validate-pack Examples/PetPack/ExamplePet.polypals-pack
```

安装、启停、更新和删除全程不访问网络。内容包删除后，用户已收藏的卡片快照仍保留。
