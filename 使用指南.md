# PolyPals 0.4 使用指南

## 安装或更新

1. 先从菜单栏脚印图标退出正在运行的 PolyPals。
2. 打开 `PolyPals-0.4.0-local.dmg`，将 PolyPals 拖入“应用程序”并选择替换旧版。
3. 只从“应用程序”保留一份 PolyPals，不要同时打开 DMG 中的副本。
4. 首次打开本地测试版时，如果 macOS 拦截，在“系统设置 → 隐私与安全性”中选择仍要打开。

0.3 会原地升级 0.2 的聊天、记忆、位置、计划和熟悉度。升级前会自动创建数据库备份，新增字段放在伴随实体中，不会清空原数据。

## 日常互动

- 单击宠物：打开快捷菜单，可聊天、要一个小套餐、送礼物或让它今天安静。
- 双击宠物：打开完整面板，包含“现在、聊天、背包、计划、角色”。
- 右键宠物：休眠、调整大小、切换主动模式或暂时隐藏。
- 拖动宠物：移动到任意屏幕；靠近边缘时会自动吸附，下次启动恢复位置。
- 单击“聊一下”会打开该宠物专属的小聊天框；Sol、Mousse、Ash 的颜色和布局不同。聊天框支持编辑重发、停止生成、重试、收藏编辑和“本次不保存”。
- 菜单栏脚印：唤醒宠物、打开设置、开启演示模式，或启动 25/50 分钟专注计时器。

## 卡片和熟悉度

- 每次套餐为 1–3 张，默认 2 张；126 张指的是轮换内容池，不是一次要完成的数量。反馈“喜欢/太简单/太难/少看相似”会调整后续选卡。
- 没有 API Key 或断网时会直接使用 126 张审核卡片。
- “本周遇见过”只是轻量回顾，不是待办列表。AI 卡片每个套餐最多补充 1 张，并且要通过质量门槛后才会缓存。
- 完成聊天、卡片、收藏、共同记忆、送礼或接受邀请会增加该宠物的熟悉度。
- 跳过、拒绝、隐藏和休眠不扣分，熟悉度不衰减，也不影响语言难度。

## 配置 OpenAI 或 DeepSeek

1. 打开菜单栏脚印中的“设置”，或按 `Command + ,`。
2. 选择 OpenAI 或 DeepSeek，再选模型和推理强度。
3. 粘贴对应服务的 API Key，点击“保存”，再点击“测试连接”。
4. 两家密钥分开保存在本机钥匙串；切换提供商不会串用密钥。
5. 开启“智能补充卡片”后，每个套餐最多联网生成 1 张非文化卡；可随时关闭。

也可以在“内容”区导入 JSON 内容包。格式示例见 `IMPLEMENTATION.md`；文化卡必须带来源，所有卡片时长必须为 30–120 秒。

## 计划与防打扰

- 在“计划”里输入“每周三下午六点让 Mousse 带来复习”，先查看解析预览，再确认添加。
- 计划支持每天/每周、桌宠动作或系统通知；通知可“暂缓一小时”“今天跳过”或取消。
- 设置中的“今天只见”只影响当天；全局每日主动邀请上限默认 2 次，可调为 0–6 次。
- 执行历史只记录触发结果和抑制原因，不记录聊天正文。演示模式、安静时间、全局冷却和全屏会明确显示原因。

聊天和已确认记忆会保存在本机。只有用户当前消息、最近聊天和用户确认的记忆会发送给当前模型服务，且请求明确设置 `store: false`。
